[package]
name = "revvy02/retained-scope"
description = "Immediate-mode traversal over a retained node tree — push/pop/peek/prune with reconciliation hooks"
version = "0.1.0"
license = "MIT"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

exclude = ["**"]
include = ["src", "src/**", "wally.toml", "default.project.json", "README.md", "LICENSE"]
